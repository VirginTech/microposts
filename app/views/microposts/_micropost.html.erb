<li id="micropost-<%= micropost.id %>">
  <%= link_to gravatar_for(micropost.user, size: 50), micropost.user %>
  <span class="user"><%= link_to micropost.user.name, micropost.user %></span>
  <span class="content"><%= micropost.content %></span>
  <span class="timestamp">
    Posted <%= time_ago_in_words(micropost.created_at) %> ago.
    
    <!--削除リンク-->
    <% if current_user == micropost.user %>
      [<%= link_to "削除", micropost, method: :delete,
                                       data: { confirm: "You sure?" } %>]
    <% end %>
    
    <!--お気に入り-->
    <% if current_user.bookmarking?(micropost) %>
      [<%= link_to "お気に入り解除", bookmark_path(micropost, redirect_url: request.url), method: :delete %>]
    <% else %>
      [<%= link_to "お気に入り登録", bookmarks_path(post_id: micropost.id, redirect_url: request.url), method: :post %>]
    <% end %>
  </span>
</li>